{% extends 'integration/base_integration.html.twig' %}

{# Force cache bust 1 #}
{% block title %}Epreuve d'Intégration{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('build/integration-classic.css') }}">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <!-- Load Pyodide (CDN for simplicity) -->
    <script src="https://cdn.jsdelivr.net/pyodide/v0.25.0/full/pyodide.js"></script>
    <script src="{{ asset('build/runtime.js') }}"></script>
    <script src="{{ asset('build/integration-classic.js') }}"></script>
{% endblock %}

{% block main %}
<div class="leetcode-container">
    <!-- Left Side: Problem Description -->
    <div class="problem-pane">
        <div class="panel-header">
            <h2>Énigme #1 : La Somme des Anciens</h2>
        </div>
        <div class="panel-content">
            <p>
                Bienvenue, jeune initié. Pour prouver ta valeur, tu dois résoudre ce premier mystère.
            </p>
            <p><strong>Description :</strong></p>
            <p>
                On vous donne un tableau d'entiers `nums` et un entier `target`.
                Votre mission, si vous l'acceptez, est de trouver les indices des deux nombres dont la somme est égale à `target`.
            </p>
            <p><strong>Exemple :</strong></p>
            <pre style="background: rgba(128, 128, 128, 0.1); padding: 10px; border-radius: 4px;">
Entrée : nums = [2,7,11,15], target = 9
Sortie : [0,1]
Explication : Car nums[0] + nums[1] == 9, on retourne [0, 1].
            </pre>
            
            <div id="console-output" class="console-box">
                <div class="console-title">Sortie Python :</div>
                <pre id="output-text">En attente...</pre>
            </div>
        </div>
    </div>

    <!-- Right Side: Code Editor -->
    <div class="editor-pane">
        <div class="panel-header editor-header">
            <h3>Votre Solution</h3>
            <div class="font-size-controls">
                <button id="font-decrease" class="font-size-btn" title="Réduire la taille">A-</button>
                <span id="font-size-label">14</span>
                <button id="font-increase" class="font-size-btn" title="Augmenter la taille">A+</button>
            </div>
        </div>
        <div class="panel-content" style="display: flex; flex-direction: column; height: 100%; padding: 0;">
             <!-- Monaco Editor Container -->
            <div id="code-editor-container"></div>

            <div class="actions">
                <button id="run-btn" class="btn" disabled>Chargement...</button>
            </div>
        </div>
    </div>
</div>

<style>
    .leetcode-container {
        display: grid;
        grid-template-columns: 40% 60%;
        height: calc(100vh - 50px); /* viewport minus header */
        width: 100%;
        background-color: var(--color-primary);
        color: var(--color-font-p);
    }

    .problem-pane, .editor-pane {
        display: flex;
        flex-direction: column;
        border-right: 1px solid rgba(128, 128, 128, 0.3);
        overflow: hidden;
        min-height: 0;
    }

    .editor-pane {
        border-right: none;
    }

    .panel-header {
        background-color: var(--color-primary);
        border-bottom: 1px solid rgba(128, 128, 128, 0.3);
        padding: 10px 15px;
        flex-shrink: 0;
    }

    .panel-header h2, .panel-header h3 {
        margin: 0;
        font-size: 1.1rem;
        color: var(--color-font-title);
    }

    .panel-header h2::after, .panel-header h3::after {
        display: none;
    }

    .panel-content {
        padding: 15px;
        overflow-y: auto;
        flex: 1;
        min-height: 0;
        display: flex;
        flex-direction: column;
        color: var(--color-font-p);
    }

    /* Monaco Editor Container Style */
    #code-editor-container {
        width: 100%;
        flex: 1;
        min-height: 0;
        position: relative;
        overflow: hidden; /* Important for Monaco */
    }

    .actions {
        padding: 10px;
        background-color: var(--color-primary);
        text-align: right;
        border-top: 1px solid rgba(128, 128, 128, 0.3);
        flex-shrink: 0;
    }
    
    .console-box {
        margin-top: 20px;
        background: rgba(0,0,0,0.3);
        border-radius: 4px;
        padding: 10px;
        font-family: monospace;
        font-size: 0.9em;
    }
    .console-title {
        color: #888;
        font-size: 0.8em;
        margin-bottom: 5px;
    }
    #output-text {
        white-space: pre-wrap;
        margin: 0;
        color: #ccc;
    }

    .editor-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .font-size-controls {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .font-size-btn {
        background: rgba(128, 128, 128, 0.2);
        border: 1px solid rgba(128, 128, 128, 0.4);
        color: var(--color-font-p);
        padding: 2px 8px;
        border-radius: 4px;
        cursor: pointer;
        font-size: 0.85rem;
        font-weight: bold;
        line-height: 1.4;
    }

    .font-size-btn:hover {
        background: rgba(128, 128, 128, 0.4);
    }

    #font-size-label {
        font-size: 0.8rem;
        color: var(--color-font-p);
        min-width: 20px;
        text-align: center;
    }
</style>
{% endblock %}